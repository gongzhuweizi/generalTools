#!/usr/bin/env python
# -*- coding: UTF-8 -*-
'''
@Project ：generalTools
@File ：test123.py
@Author ：zhangyi
@Date ：2022/5/20 3:42 PM
'''






# dom算是指代xml文件的对象 xml_path为xml的存储路径
# root_x = dom.getElementsByTagName("descript")  # root_x是标签为services的节点列表
# hostdev1 = dom.createElement("hostdev")  # 创建一个标签为hostdev的节点
# hostdev1.setAttribute('mode', 'subsystem')  # 该节点是属性值设置为mode=subsystem
# hostattr1 = dom.createAttribute('type')  # 创建一个新属性值，名字为type
# hostdev1.setAttributeNode(hostattr1)  # 将该属性值加入节点hostdev1中
# hostdev1.setAttribute('type', 'pci')  # 将属性type的值设为pci

# 保存内容到file中
# file：要保存为的文件对象名
# indent：根节点的缩进方式
# allindent：子节点的缩进方式
# newl：针对新行，指明换行方式
# encoding：保存文件的编码方式
# fp = open(xml_path, 'w', encoding='utf-8')
# dom.writexml(fp, indent='', addindent='\t', newl='\n', encoding='utf-8')
# fp.close()










